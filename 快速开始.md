# 快速开始 — 零基础安装与使用

> 面向第一次使用的用户，手把手带你从"拿到文件"到"屏幕上出现大字"。
> 遇到问题先看 [常见问题](#常见问题faq)；需要脚本调用见 [`API调用说明.md`](API调用说明.md)。

---

## 这是什么？

一个 Windows 后台小工具：当**指定程序启动**、**程序自己启动**或**你手动触发**时，在屏幕所有窗口之上浮现一张仿《Control》风格的**白色大字标题**，停留几秒后淡出。它不打断你操作，也不联网。

---

## 一分钟上手

1. 解压发布包，进入文件夹，**双击 `Object1688.Main.exe`**。
2. 看右下角托盘出现图标 → **右键 →「设置…」** 打开配置窗口。
3. 点「新建向导…」，填：匹配值 `chrome.exe`、文字 `浏览器` → 完成 → 保存。
4. 打开 Chrome，几秒后屏幕中央出现"浏览器"大字。**搞定！**

---

## 系统要求

| 项 | 要求 |
|----|------|
| 系统 | Windows 10 / Windows 11（64 位） |
| 运行库 | **无需安装**（程序自带运行环境） |
| 磁盘 | 约 700MB（自包含，含运行环境，解压后） |

---

## 安装（详细）

1. **获取发布包**：从 GitHub Releases 下载 `.zip` 并解压（或自行构建，见 `README`）。
   解压后应看到这些文件（**缺一不可，需放在同一文件夹**）：
   `Object1688.Main.exe`、`Object1688.Overlay.exe`、`Object1688.Monitor.exe`、`Object1688.Logging.exe`、`Object1688.ConfigUI.exe`，以及 `config/`、`build/` 文件夹。
2. **放到任意文件夹**：例如 `C:\Object1688`。请**整个文件夹一起保留**，不要只复制单个 exe。
3. **无需安装器**：绿色软件，双击 `Object1688.Main.exe` 即可运行。

> ⚠️ 若 Windows 弹出 **SmartScreen"未知发布者"**：本程序未做商业代码签名，属正常现象。点击 **"更多信息 → 仍要运行"** 即可。

---

## 首次运行与托盘

- 程序**没有主窗口**，启动后常驻右下角**系统托盘**；首次启动会弹气泡引导。
- 首次启动会显示一次"欢迎大字"（可在配置窗口关闭）。
- **托盘操作**：

| 操作 | 效果 |
|------|------|
| 左键单击 | 快捷菜单（手动大字 / 暂停·恢复） |
| 双击 | 打开配置窗口 |
| 右键 | 完整菜单（设置/手动大字/性能窗口/切换语言/关于/导出诊断包/打开日志/复制最近错误码/退出） |
| 悬停 | 显示运行状态（运行中/已暂停/勿扰中/有错误） |

> ⚠️ **Win11 提示**：新出现的托盘图标可能被收进右下角 **`^`「隐藏的图标」**——用鼠标拖到任务栏即可固定。若一时找不到图标想退出程序，**双击发布目录下的 `build\quit.cmd`** 即可（或托盘右键 →「退出」）。

---

## 添加第一条规则（图文步骤）

1. 托盘右键 →「设置…」→ 顶部工具栏「**新建向导…**」。
2. **第 1 步 匹配**：
   - 匹配类型：**进程名**（如 `chrome.exe`）或 **窗口标题**（如 `我的游戏`）。
   - 匹配模式：**完全** / **包含** / **通配**（`*` 任意多字符、`?` 单字符，如 `*游戏*`）。
   - 匹配值：填入要识别的进程名或标题。
3. **第 2 步 显示**：填写要显示的文字（可多行），设置字号、延时、保持秒数。
   - 文字支持占位符：`{appName}`（命中进程名）、`{time}`（当前时间）。
4. **第 3 步 确认** →「完成」→ 回主界面点「**保存**」。
5. 可先点「**测试显示**」在真实屏幕试看效果。

> 想微调样式？在「触发规则」页可设描边（防白底撞色）、位置，或直接套用「**样式预设**」（Control 经典/高对比/字幕风/影院深底），也可以**用鼠标拖拽预览里的文字**设置自定义位置。

### 描边色 / 描边宽度怎么填？

- **描边色**（给白色大字加一圈边，防止在浅色背景上看不清）：
  - 点「**常用色**」色块**一键选择**（黑 / 白 / 红 / 蓝 / 黄 / 绿 / 深灰）；
  - 或点「**选择颜色…**」打开 **Windows 系统取色器**，任意选色；
  - 进阶：也可**手动输入** `#RRGGBB`（如 `#000000` 黑、`#FFFFFF` 白、`#FF0000` 红）；
  - **留空 = 跟随全局默认**（全局默认在「勿扰/提示音/自启」页设置）；点「**跟随全局**」即清空。
- **描边宽度**：数字（像素），`0` = 不描边；一般填 **2–6** 即可。
- **描边方式**：**柔光**（方案A，开销低、边缘柔和）/ **精确**（方案B，实心描边、更清晰醒目）。

> 小技巧：白字 + 黑描边几乎在任何背景上都清晰；深色背景可改用浅色描边或直接不描边。

---

## 常用功能速查

| 想做什么 | 怎么做 |
|----------|--------|
| 手动弹一次大字 | 托盘右键「手动大字」或按快捷键（默认 `Alt+F`） |
| 提前结束大字 | 再次按手动快捷键，或脚本 `--control end` |
| 临时不弹（暂停） | 托盘左键/右键「暂停」；再点「恢复」 |
| 定时勿扰 | 配置窗口「勿扰/提示音/自启」页设时段 |
| 投屏/演示时自动不弹 | 同页开启「投屏/演示时自动静默」 |
| 换手动快捷键 | 配置窗口「手动大字」页「快捷键」框 |
| 改欢迎大字 | 配置窗口「欢迎大字」页 |
| 换界面语言 | 托盘右键「切换语言」 |
| 看性能/事件 | 托盘「性能窗口…」 |
| 开机自启开关 | 配置窗口「勿扰/提示音/自启」页 |
| 一键诊断包（反馈用） | 托盘「导出诊断包…」或脚本 `--control diagnostics` |

---

## 数据与隐私

- **纯本地运行，不联网、不上传任何数据。**
- 配置：`%APPDATA%\Object1688\config.json`；统计：`stats.json`；日志：程序目录 `log/`。
- 日志/事件流含命中的进程名与窗口标题（仅用于排障），可一键清除。

---

## 卸载

1. 托盘右键 →「退出」（或"退出并取消开机自启"）；若找不到托盘图标，**双击 `build\quit.cmd`**。
2. **双击发布目录下的 `build\uninstall.cmd`**（删除开机自启项 + 清理 `%APPDATA%\Object1688`；命令行加 `-AlsoCleanLogs` 连日志一起清）。
3. 或手动删除程序文件夹与 `%APPDATA%\Object1688`。

---

## 常见问题（FAQ）

**Q：双击没反应 / 一闪而过？**
A：确认是双击 `Object1688.Main.exe`，且同目录存在另外四个 `Object1688.*.exe`。程序无主窗口，启动后请查看右下角托盘。

**Q：屏幕没有出现大字？**
A：① 规则是否「启用」；② 匹配值是否正确；③ 是否处于"暂停/勿扰"；④ 打开「性能窗口」看事件流是否有命中记录。

**Q：中文显示成方框？**
A：中文依赖系统字体（微软雅黑等），正常会自动回退；若仍异常请反馈。

**Q：全屏游戏里不显示？**
A：部分"独占全屏"游戏无法覆盖，程序会尽力置顶并在失败时记录日志降级，不打断游戏。

**Q：提权程序（以管理员运行的程序）上不显示？**
A：本程序以普通权限运行，无法覆盖管理员窗口；命中时会托盘提示一次（OVL-W-3008）。

**Q：快捷键没反应？**
A：可能被其他软件占用——程序会托盘提示，去配置窗口「手动大字」页换一个快捷键。

**Q：想反馈问题？**
A：托盘「导出诊断包…」生成 `log/exports/diagnostics-*.zip`（日志+配置+统计+环境），附上它即可。

---

## 更多文档

- 完整手册（中/英）：`docs/MANUAL[v1.1.0].md` / `docs/MANUAL.en[v1.1.0].md`
- **脚本调用接口**：[`API调用说明.md`](API调用说明.md)
- 项目说明：`docs/README[v1.1.0].md`
