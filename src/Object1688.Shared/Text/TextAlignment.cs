namespace Object1688.Shared.Text;

/// <summary>
/// 大字行对齐方式（F-06：每行独立对齐，缺省回退块级默认 = Center）。
/// </summary>
public enum TextAlignment
{
    /// <summary>左对齐。</summary>
    Left,

    /// <summary>水平居中（默认）。</summary>
    Center,

    /// <summary>右对齐。</summary>
    Right,
}